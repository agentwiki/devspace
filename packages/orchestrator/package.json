{
  "name": "@devspace/orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Control plane: work-unit FSM, event routing, secret resolution, git/PR wrapper",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@devspace/contracts": "workspace:*",
    "@devspace/db": "workspace:*",
    "@devspace/sandbox-core": "workspace:*",
    "@devspace/agent-runner": "workspace:*",
    "@devspace/chat-gateway": "workspace:*"
  }
}
