{
  "name": "@devspace/orchestrator-svc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deployable entrypoint for the orchestrator (control plane) service",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@devspace/contracts": "workspace:*",
    "@devspace/db": "workspace:*",
    "@devspace/orchestrator": "workspace:*",
    "@devspace/sandbox-core": "workspace:*",
    "@devspace/agent-runner": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  }
}
